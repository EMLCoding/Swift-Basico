import UIKit

// OPEN Y PUBLIC: Podemos acceder a ese objeto desde cualquier parte del programa y fuera de la app
// INTERNAL: Podemos acceder solo desde dentro del modulo, es decir, en swift dentro de la app que estamos construyendo
// FILEPRIVATE: Podemoss acceder al objeto solo desde el fichero en el que nos encontramos
// PRIVATE: Podemos acceder al objeto solo desde el contexto en el que está definido (bloque de código delimitado por {})
